{
  "name": "jsonlens",
  "version": "1.0.0",
  "private": true,
  "description": "A privacy-first, client-side JSON viewer",
  "scripts": { "dev": "vite", "build": "vite build", "preview": "vite preview" },
  "devDependencies": { "vite": "latest" }
}
